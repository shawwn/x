'use strict';

civ.unit_types = [];

var index = 0;

civ.UNIT_TYPE__MOTHERSHIP 			= index; index++;

civ.UNIT_TYPE__TRANSPORT_HELICOPTER	= index; index++;
civ.UNIT_TYPE__TRANSPORTER_TRUCK 	= index; index++;
civ.UNIT_TYPE__TRANSPORT_SHIP		= index; index++;

civ.UNIT_TYPE__FISHER_BOAT			= index; index++;

civ.UNIT_TYPE__ 				= index; index++;
civ.UNIT_TYPE__ 				= index; index++;
civ.UNIT_TYPE__ 				= index; index++;
civ.UNIT_TYPE__ 				= index; index++;